# Spawn a cube in the planning scene at (x,y,z) with given size, in world frame
# Request
float64 x
float64 y
float64 z
float64 size   # cube side length in meters
---
# Response
bool success
string message
string object_name   # e.g. cube_0, cube_1, ...
